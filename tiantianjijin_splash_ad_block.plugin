#!name=天天基金
#!desc=拦截天天基金 App 开屏广告（连接层直接拒绝开屏下发接口 GetAppStartImg，App 拿不到开屏数据即跳过，不展示广告）；纯本地规则，零远程脚本、不上报、不联网；若你的 App 版本未对 appactive 域做证书固定则可拦开屏，已固定则本插件优先保证首页 UI 正常
#!author=FFDNS[https://github.com/Thelongdarkorg/loon-plugins]
#!icon=https://raw.githubusercontent.com/Thelongdarkorg/loon-plugins/main/icons/tiantianjijin.jpg

# 说明：本插件全部为 Loon 原生规则（连接层拦截），不含任何远程脚本。
# 开屏链路（2026-09-18 实测取证）：App 请求 appactive.1234567.com.cn 的
#   /AppoperationApi/OperationService/GetAppStartImg 取开屏数据，其中 ImgUrl
#   指向 j5.dfcfw.com 的 /WG/conf/<8 位日期>/N<32 位十六进制>.png。
#
# 拦截方式：Rule 层 URL-REGEX 在连接层直接拒绝该 URL。
#   - 若 Loon 的 MITM 对该域生效（即 App 未对 appactive 做证书固定），URL-REGEX
#     能看到完整路径 → 拒绝开屏接口 → App 跳过开屏，无广告。
#   - 若 App 对 appactive 做了证书固定（金融 App 常见），MITM 自签证书通不过校验，
#     Loon 看不到路径、URL-REGEX 不命中 → 开屏照常；此时若改「域名级 REJECT」
#     会连坐同源的行情/交易/自选/首页配置（整个 App 断网），故本插件不采用。
#   - 不拦截 j5.dfcfw.com 图片域：该目录是运营位共用 CMS，首页图标同源、命名一致，
#     图片级拦截会误伤主页图标（已踩坑，2026-09-18 修正）。
# MITM：依赖 Loon 全局 MITM（信任 CA 后自动解密）。本插件不单独声明 MITM 域，
#   避免对共用图片 CDN 做无谓解密。

[Rule]
# ---------- 开屏广告下发接口：连接层直接拒绝 ----------
# 精确只匹配 GetAppStartImg 这一条路径，不影响同域的行情/交易/首页配置接口
URL-REGEX, ^https?://appactive\.1234567\.com\.cn/AppoperationApi/OperationService/GetAppStartImg, REJECT
# 该域 QUIC 走 UDP（MITM 解不了、URL-REGEX 也匹配不到）；挡掉让开屏请求回退 TCP，
# 上面的 URL-REGEX 才能命中。挡整个域的 QUIC 不影响功能——其它路径回退 TCP 后正常
AND,((DOMAIN-SUFFIX,appactive.1234567.com.cn),(PROTOCOL,QUIC)),REJECT
